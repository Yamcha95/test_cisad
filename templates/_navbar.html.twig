<nav class="navbar navbar-expand-lg navbar-dark bg-dark shadow-sm">
    <div class="container">
        <a class="navbar-brand fw-bold" href="{{ path('app_home') }}">
            <i class="bi bi-shield-check"></i> Test CISAD
        </a>
        
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#mainNav">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="mainNav">
            <ul class="navbar-nav me-auto">

                {% if app.user %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_profile') }}">Mon Profil</a>
                    </li>

                    {# Menu réservé à l'ADMIN #}
                    {% if is_granted('ROLE_ADMIN') %}
                        <li class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle text-info" href="#" role="button" data-bs-toggle="dropdown">
                                Administration
                            </a>
                            <ul class="dropdown-menu">
                                <li><a class="dropdown-item" href="{{ path('app_user_index') }}"><i class="bi bi-people"></i> Utilisateurs</a></li>
                                <li><a class="dropdown-item" href="{{ path('app_infos_index') }}"><i class="bi bi-graph-up"></i> Statistiques Jeu</a></li>
                                <li><hr class="dropdown-divider"></li>
                                <li><a class="dropdown-item" href="{{ path('app_user_new') }}"><i class="bi bi-person-plus"></i> Ajouter un membre</a></li>
                            </ul>
                        </li>
                    {% endif %}
                {% endif %}
            </ul>

            <div class="navbar-nav">
                {% if app.user %}
                    <span class="navbar-text me-3 d-none d-lg-inline">
                        Connecté en tant que <strong>{{ app.user.userIdentifier }}</strong>
                    </span>
                    <a href="{{ path('app_logout') }}" class="btn btn-outline-danger btn-sm">Déconnexion</a>
                {% else %}
                    <a href="{{ path('app_login') }}" class="btn btn-outline-light btn-sm me-2">Connexion</a>
                    <a href="{{ path('app_register') }}" class="btn btn-primary btn-sm">Inscription</a>
                {% endif %}
            </div>
        </div>
    </div>
</nav>